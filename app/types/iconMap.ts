// iconMap.ts
import { IconType } from "react-icons";
import { BiAlarm, BiUser } from "react-icons/bi"; // Import example icons
// Add all required icons here

const iconMap: { [key: string]: IconType } = {
  BiAlarm,
  BiUser,
  // Add other icons here
};

export default iconMap;
